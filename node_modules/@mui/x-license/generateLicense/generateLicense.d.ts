import { LicenseScope, PlanVersion } from '@mui/x-license/utils/licenseScope';
import { LicensingModel } from '@mui/x-license/utils/licensingModel';
export interface LicenseDetails {
    orderNumber: string;
    expiryDate: Date;
    scope: LicenseScope;
    licensingModel: LicensingModel;
    planVersion: PlanVersion;
}
export declare function generateLicense(details: LicenseDetails): string;
